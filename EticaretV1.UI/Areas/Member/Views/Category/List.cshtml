
@{
    ViewBag.Title = "List";
    Layout = "~/Areas/Member/Views/Shared/_Layout.cshtml";
}

@model IEnumerable<EticaretV1.Model.Option.Category>

<div class="col-md-12">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Kategori Adı</th>
                <th>Açıklaması</th>
                <th colspan="1">Ürünleri Git</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Description</td>
                    <td>
                        <a href="/Member/Product/List/@item.Id">Ait Ürünleri Gör!</a>
                       
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>